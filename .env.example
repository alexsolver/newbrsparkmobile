# Copie para `.env` na mesma pasta e ajuste. O ficheiro `.env` não vai para o git.
# API BrSpark (origem sem /api no fim). Em dev com Expo Go, o app usa isto em vez do IP local.
EXPO_PUBLIC_API_BASE=https://brsparks.wstrategy.com.br
