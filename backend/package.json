{
  "name": "brspark-api",
  "version": "2.0.0",
  "private": true,
  "description": "API única: PostgreSQL via admin-panel/backend (Prisma). SQLite foi removido.",
  "scripts": {
    "start": "npm --prefix ../admin-panel/backend run start",
    "dev": "npm --prefix ../admin-panel/backend run dev",
    "db:migrate": "npm --prefix ../admin-panel/backend run db:migrate",
    "db:seed": "npm --prefix ../admin-panel/backend run db:seed",
    "db:studio": "npm --prefix ../admin-panel/backend run db:studio"
  }
}
